import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Nutrition = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>Nutrition Module</Text>
            <Text style={styles.description}>
                This module offers meal plans based on your goals, allergies, religion, fasting, and price/quality considerations.
            </Text>
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20,
    },
    title: {
        fontSize: 24,
        fontWeight: 'bold',
    },
    description: {
        fontSize: 16,
        textAlign: 'center',
        marginTop: 10,
    },
});

export default Nutrition;